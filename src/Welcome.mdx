import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Welcome" />

<div className="tip-magic-welcome">

<div className="tip-magic-hero">
  <h1>✨ React Tip Magic</h1>
  <p className="subtitle">Thoughtfully crafted tooltips for React</p>
</div>

## Welcome

React Tip Magic is a tooltip library built with care. It handles the small details that make interfaces feel polished—smooth transitions, proper positioning, keyboard accessibility—so you can focus on building your application.

Whether you need simple tooltips, guided tours, or a way for users to discover keyboard shortcuts, React Tip Magic provides clean, declarative APIs that stay out of your way.

---

## Features

<div className="tip-magic-features">
  <div className="tip-magic-feature">
    <h3>🎯 Tooltips</h3>
    <p>Add <code>data-tip="Hello"</code> to any element. One tooltip instance moves gracefully between targets.</p>
  </div>

  <div className="tip-magic-feature">
    <h3>⌨️ Keyboard Shortcuts</h3>
    <p>Display shortcuts with <code>data-tip-shortcut="⌘S"</code>. Styled badges that feel native to your UI.</p>
  </div>

  <div className="tip-magic-feature">
    <h3>🗺️ Guided Tours</h3>
    <p>Walk users through your interface step by step. Great for onboarding or introducing new features.</p>
  </div>

  <div className="tip-magic-feature">
    <h3>🔍 Tip Advisor</h3>
    <p>A searchable menu of all shortcuts in your app. Press F1 to discover what's available.</p>
  </div>

  <div className="tip-magic-feature">
    <h3>🎨 Smooth Transitions</h3>
    <p>Tooltips animate between elements. Group related items for cohesive movement.</p>
  </div>

  <div className="tip-magic-feature">
    <h3>📱 Accessible</h3>
    <p>Keyboard navigation, screen reader support, and respects reduced motion preferences.</p>
  </div>

  <div className="tip-magic-feature">
    <h3>🚀 Performant</h3>
    <p>Single global instance with event delegation. Minimal re-renders and memory footprint.</p>
  </div>

  <div className="tip-magic-feature">
    <h3>🎭 Themeable</h3>
    <p>CSS custom properties for easy customization. Adapts to light, dark, or custom themes.</p>
  </div>
</div>

---

## Quick Start

### Installation

```bash
npm install @galangel/react-tip-magic
```

### Setup

```tsx
import { TipMagicProvider } from '@galangel/react-tip-magic';
import '@galangel/react-tip-magic/styles.css';

function App() {
  return (
    <TipMagicProvider>
      <YourApp />
    </TipMagicProvider>
  );
}
```

### Add Tooltips

```tsx
<button data-tip="Save your work">Save</button>

{/* With keyboard shortcut */}
<button data-tip="Copy" data-tip-shortcut="⌘C">Copy</button>
```

---

## Architecture

React Tip Magic uses a global singleton pattern:

1. Mount `<TipMagicProvider>` at your app's root
2. Add `data-tip` attributes to elements
3. Event delegation handles interactions
4. A single tooltip moves between targets

This means:
- Minimal DOM nodes
- Smooth transitions between tooltips
- Works with dynamically added elements
- No wrapper components in your JSX

---

## Explore

Use the sidebar to learn more:

- **[The Tooltip](?path=/story/the-tooltip--the-tooltip)** — Interactive playground
- **[Keyboard Shortcuts](?path=/docs/tooltip-keyboard-shortcuts-documentation--docs)** — Displaying shortcuts
- **[The Tour](?path=/story/the-tour--the-tour)** — Guided walkthroughs
- **[Tip Advisor](?path=/docs/tooltip-tipadvisor-documentation--docs)** — Shortcut discovery

---

<p style={{ textAlign: 'center', color: 'var(--tip-magic-text-muted)', marginTop: '3rem' }}>
  Made with care for the React community
</p>

</div>
